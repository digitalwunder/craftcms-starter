<section class="af-image-text">
  <div class="container">
    <div class="row align-items-center">
      <div class="col-lg-6">
        {% include '_atoms/image' with {image: block.image} %}
      </div>
      <div class="col-lg-5 offset-lg-1">
        <h2>{{block.headline}}</h2>
        <div class="af-image-text_content-text">
          {{block.text}}
        </div>
        <div class="af-image-text_content-list">
          <ul class="list-unstyled">
            {% for item in block.list %}
              <li>✓ {{item.text}}</li>
            {% endfor %}
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>
